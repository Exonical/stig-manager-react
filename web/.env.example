# API base URL used by the SPA at runtime. Default '/api' relies on the Vite
# dev proxy in vite.config.ts (or a reverse proxy in prod).
VITE_API_BASE_URL=/api

# Where the Vite dev proxy forwards /api requests. Match the Go API server.
VITE_API_PROXY_TARGET=http://localhost:54001
